main() {
    name := "windows"
    output := "./windows.Dockerfile"
    if (name == "windows") {
        {{-
            FROM windows
        -}}
    } else {
        {{-
            FROM linux
        -}}
    }
}