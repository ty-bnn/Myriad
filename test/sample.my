main() {
    data := JsonUnmarshal("./test/versions.json")

    for (version in data.keys) {
        versionData := data[version]

        for (variant in versionData["variants"]) {
            version := "version"
            CMD {{versionData[version]}}
        }
    }

    CMD [hfsdf]
}