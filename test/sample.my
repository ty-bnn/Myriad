main() {
    {{-
        FROM ubuntu:latest
        COPY aaa aaa
        RUN go build -o . \
    -}}

    data := JsonUnmarshal("./test/versions.json")
    versionData := data["18"]
    aaa := "./" + "hello/ " + versionData["version"]
    {{- {{aaa}} -}}
}