hello(p) {
    {{-
        RUN {{p[0]}}
    -}}
}

main() {
    {{-
        FROM ubuntu:latest
        COPY aaa aaa
        RUN go build -o .
    -}}

    packages := {"aaa", "bbb", "ccc"}
    hello(packages)
}