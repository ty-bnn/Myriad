main() {
    {{-
        FROM ubuntu:latest
        COPY aaa aaa
        RUN go build -o . \
            aaa
    -}}

    package := "curl"
    if(package == "curl") {
        {{- {{package}} -}}
    }
}