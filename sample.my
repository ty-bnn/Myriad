hello(package) {
    if (package == "helo") {
        RUN {{package}}
    } else if (package == "hello") {
        RUN {{package}}
    }
}

abc(pack) {
    hello(pack)
}

main()
{
    version := "1.1.1"
    FROM {{version}}
    abc("hello")
    RUN good \
        {{version}} \
        morning
    CMD [hfsdf]
}