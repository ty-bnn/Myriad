abc(package) {
    if (package == "hello") {
        RUN {{package}}
    }
}

main()
{
    version := "1.1.1"
    FROM {{version}}
    abc("hello")
    RUN good \
        {{version}} \
        morning
    CMD [hfsdf]
}