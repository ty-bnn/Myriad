abc(package) {
    if (package == "helo") {
        RUN aaa
    } else if (package == "hel") {
        RUN {{package}}
    } else {
        if (package == "hel") {
            RUN bbb
        } else if (package == "he"){
            RUN ccc
        }
    }
}

main()
{
    version := "1.1.1"
    FROM {{version}}
    abc("hello")
    RUN good \
        {{version}} \
        morning
    CMD [hfsdf]
}